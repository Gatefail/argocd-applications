apiVersion: v1
kind: Service
metadata:
  name: alpha-metric-spammer-1-svc
  namespace: project-alpha
  labels:
    name: alpha-metric-spammer-1-svc
spec:
  ports:
    # the port that this service should serve on
  - port: 9001
    targetPort: 8001
  selector:
    name: alpha-metric-spammer-1